/** Maximum file size allowed for conversion, in megabytes. */
export const MAX_FILE_SIZE_MB = 200;

/** Same limit expressed in bytes for use in size checks. */
export const MAX_FILE_SIZE_BYTES = MAX_FILE_SIZE_MB * 1024 * 1024;
